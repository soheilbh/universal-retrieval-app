# Universal Data Retrieval App
# Run: docker-compose up -d

services:
  universal-retrieval:
    build: .
    container_name: universal-retrieval
    restart: unless-stopped
    ports:
      - "8502:8502"
    volumes:
      - retrieval_outputs:/app/outputs

volumes:
  retrieval_outputs:
